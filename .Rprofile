setwd("~/Library/CloudStorage/Dropbox/Developer/hello_world")
setHook("rstudio.sessionInit", function(newSession) { 
    if (newSession) {
    rstudioapi::applyTheme("Nord Polar Night Aurora {rsthemes}")
    }                                                           
}, action = "append")
